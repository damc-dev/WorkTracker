{
  "name": "electron-task-tracker",
  "version": "1.0.0",
  "description": "Quick Look at Completed Work",
  "author": "David McElligott",
  "dependencies": {
    "angular": "^1.5.8",
    "angular-animate": "^1.5.8",
    "angular-aria": "^1.5.8",
    "angular-material": "^1.0.9",
    "angular-route": "^1.5.8",
    "electron-debug": "^1.0.1",
    "menubar": "^5.1.0",
    "moment": "^2.14.1",
    "request": "^2.74.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "devtron": "^1.3.0",
    "electron-packager": "^5.2.1",
    "electron-prebuilt": "^0.36.0"
  },
  "main": "main.js",
  "scripts": {
    "start": "electron . --debug=5858",
    "build": "electron-packager . TimeTracker --all --out dist/ --version=0.36.0 --overwrite --icon=Icon.icns",
    "build-win": "electron-packager . TimeTracker --platform=win32 --arch=all --out dist/ --version=0.36.0 --overwrite --icon=Icon.icns",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
